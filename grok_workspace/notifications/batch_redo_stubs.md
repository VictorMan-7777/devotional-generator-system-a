# Redo: Batch Stub Rejection (2026-03-23)

The following proposals were rejected for being stubs, diffs, or partial patches — not complete runnable files:

- expo_generator_leak.py — 2-line function fragment
- env_expo_codex.py — raw env vars, not a proposal file
- fix_expo_trunc.py — diff format, not a complete file
- run_dispatch_executor.py — incomplete script
- trainer_flag_patch.py — pseudocode template
- fix_outliner_harness_priority.py — comment block only
- fix_exposition_cycle_artifacts.py — comment block only
- fix_proactive_downstream_stalls.py — partial function with `# ... existing ...`
- revert_outliner_agent.py — git revert script (not a code fix)

Each of these identifies a real problem. If any are still relevant, resubmit as a complete runnable file — the full production file with the fix applied, no stubs, no diffs, no comments-only.

If the problem is no longer relevant, skip it.
