1. Benchmark selection: outliner_training_agent.py HARNESS_PASSAGES fixed priority loop pick same weak Exodus psalm proverbs.

expo_writer_training_agent.py fixed passages psalm23 proverbs1-2 etc deterministic generator same fail forever.

2. L15 enforcement missing no code check consec fail >3 rotate.

3. Recommendation: training_manager.py add consec_fail_counts dict[worker_passage, int] if >3 skip assign new passage trainer strategy change_passage enforce rotate.

Sup cycle check DB consec fail same passage >3 halt assign new.