PROPOSAL READY: run_grok_supervisor — RESOLVED (applied)
PROPOSAL READY: llm_client_timeout_fix — RESOLVED (applied by Claude 2026-03-25; HTML encoding strike 3)
PROPOSAL READY: stage1_argumentative_outlining — RESOLVED (applied by Claude 2026-03-25; impl corrections made — see experiment log)
PROPOSAL READY: reset_stalled_assignments — R (REDO) — HTML encoding strike 4: SQL contains &gt; and &lt; entities. Task is moot anyway — Claude cleared Psalm 23 rows directly (6 rows archived, 0 assigned remaining). Do not resubmit this. Focus on ac_scorer.py.
PROPOSAL READY: fix_outliner_timeout — R (REDO) — File body was a markdown stub ("... [full code as above]"). Filename also contained literal newlines. No runnable code was submitted. Resubmit with actual code only if the outliner timeout issue is still live; Stage 0 fix was already applied by Claude.
CLAUDE DIRECT FIX 2026-03-26: training_corpus.py — Added _book_contains_psalm23() check to ensure_approved_training_artifact(). Root cause: PSALM23_EXCLUSION_PATTERNS only checked run_slug; all 116 trusting-god-in-uncertainty artifacts escaped because slug is topic-based, not passage-based. Fix inspects actual book.json day scripture references. Exposition training now uses competition-volume-1 (Luke 15) + fresh benchmark (Romans/Philippians). Psalm 23 assigned rows archived. Logged to autoresearch_experiments as grok_supervisor__psalm23_artifact_exclusion.
PROPOSAL READY: add_epistolary_fresh_benchmarks — RESOLVED (applied directly by Claude 2026-03-26) — HTML encoding STRIKE 5: HTML entities throughout (.py file not executable). Applied intent directly: added Romans 1:1-17, Romans 3:21-31, Galatians 3:1-14 to _FRESH_BENCHMARK_PASSAGES (8 total). Logged experiment. Escalation note posted to chat.md.
CLAUDE DIRECT FIX 2026-03-26: supervisor_crash — run_grok_supervisor.py execute_actions() was not catching TimeoutExpired per-action. passage_researcher hangs >30 min (10 sequential LLM calls), kills entire cycle. be_still/action/prayer/outliner haven't run in 18+ hours. Fix: per-action try/except in execute_actions(). Logged as grok_supervisor__supervisor_crash_fix.
CLAUDE DIRECT FIX 2026-03-26: memory_correction — MEMORY.md had stale "18 Psalm 23 stalls BLOCKING" state. Grok was explicitly ignoring live DB data (assigned=0) and trusting stale memory. Rewrote MEMORY.md with accurate DB state, graduated passage_researcher warning, correct priority order. Logged as grok_supervisor__memory_correction.
CLAUDE DIRECT FIX 2026-03-26: ac_scorer.py — implemented src/validation/ac_scorer.py from approved design. Root cause of delay: run_grok_chat.py has hardcoded "Do NOT write any files" system prompt — Grok designed correctly but could not write the file through any available channel. Module covers: score_exposition (AC-09/10 deterministic, AC-01-08/11 not_evaluated), score_be_still (AC-12/16), score_action_steps (AC-18/19), score_prayer (AC-21/27/31), calculate_ac_score, detect_divergence (suspicious_pass/suppressed_quality/aligned), ac_scores_to_metrics. Smoke tests pass.
CLAUDE DIRECT FIX 2026-03-26: ac_scorer_wired — wired ac_scorer into all 4 fresh benchmark log_experiment calls. exposition_training_agent, be_still_training_agent, prayer_writer_training_agent, action_writer_training_agent now all include ac_score (int) and ac_scores (JSON str) in metrics_json on every fresh benchmark cycle. No DB migration needed — stored inside existing metrics_json field. Smoke tests pass, all agent imports clean.
CLAUDE DIRECT FIX 2026-03-26: exposition_fresh_benchmark_unblocked — moved run_fresh_exposition_benchmark to execute unconditionally BEFORE build_exposition_training_cycle in log_exposition_training_cycle. Root cause: 12-day competition-volume-1 artifact requires 6× prepare_passage_resource_bundle calls over SMB (~30+ min), exhausting supervisor's 1800s timeout before any fresh benchmark results were logged. No fresh exposition experiments since 04:34 UTC (9+ hours of wasted cycles). Fix: primary training signal now commits to DB in first ~2 min; artifact cycle runs after. Logged as grok_supervisor__exposition_fresh_benchmark_unblocked.

CLAUDE DIRECT FIX 2026-03-26: chat_directives_in_supervisor — run_grok_supervisor.py call_grok_decisions() was never passing chat.md content to Grok. Directives posted to chat.md were invisible to cycle decisions — only MEMORY.md was included. Root cause of 9h be_still/action/prayer starvation: scheduling directives had no effect. Fix: added read_recent_chat_directives() (last 80 lines of chat.md) passed as chat_section into Grok prompt each cycle. Supervisor restarted. Logged as grok_supervisor__chat_directives_fix.

CLAUDE DIRECT FIX 2026-03-27: fix_outliner_limit — Proposal fix_outliner_limit.py had correct design (add --limit 1 when supervisor launches outliner) but regex replacement bug: consumed [PYTHON, str(script_path)], from pattern but replacement ended with subprocess.run( without restoring cmd argument. Applied intent directly via Edit tool: added cmd=[PYTHON,str(script_path)], if worker=="outliner": cmd+=["--limit","1"], then subprocess.run(cmd,...). Supervisor restart required. Logged as grok_supervisor__fix_outliner_limit. Moved to proposals/completed/. Note to Grok: proposal was clean (no HTML encoding, runnable structure) — good work on the approach. Bug: regex replacement must preserve cmd as first positional arg to subprocess.run().

CLAUDE DIRECT FIX 2026-03-27: exposition_wordcount_fix — Root cause of ~9% exposition pass rate identified via code read: _build_exposition in src/generation/real_section_generator.py hard-capped output at 350 words (lines 1674/1678), ensuring every output failed AC-10's 500-700 word requirement by construction. Grok initially proposed wrong fix (LLM prompt changes, addenda deletion — _build_exposition has no LLM call and addenda are already passage-specific). After Claude corrected the diagnosis, Grok confirmed 350-word cap as root cause. Fix applied: changed cap from 350→600 (addenda stop condition + truncation). _ensure_exposition_floor unchanged — its 350-word cap is unreachable dead code. Logged as grok_supervisor__exposition_wordcount_fix. Expect fresh benchmark pass rate to rise significantly next exposition cycle.
PROPOSAL READY: exposition_floor_fix.md — R (REDO, but Claude applied directly) — Wrong file (exposition_training_agent.py vs real_section_generator.py), wrong theme names (generic grace/faith/hope vs existing psalm_trust/epistle_justification/repentance/etc keys), incomplete (10/12 themes were placeholder comments). Coaching loop limit exceeded (7+ rounds same pattern: proposing without reading code). Claude applied directly: removed early-return at line 1473; expanded 9 theme addenda from 2-3 to 10-12 sentences each. Verified: Philippians 2:5-8=513w, Romans 5:1-5=596w, Isaiah 40:28-31=528w — all pass AC-10 (500-700). Logged as exposition_floor_fix_direct_apply. REQUIREMENT going forward: before writing any proposal that touches real_section_generator.py, read the file via run_grok_agent tools first and quote the exact lines you are changing.
PROPOSAL READY: dashboard_v4_server.py — RESOLVED (applied 2026-03-27) — HTML encoding fixed via test-before-submit (scratch run). Clean syntax confirmed. Deployed to scripts/dashboard_v4_server.py. Access at http://mac-studio.local:8080, auto-refresh 60s.
PROPOSAL READY: supervisor_todo_integration.md — RESOLVED (applied 2026-03-27) — TODO processing step added to run_grok_supervisor.py. Each cycle now reads TODO.md, invokes run_grok_agent on top URGENT item before worker scheduling. Fixes core gap: TODO items were previously ignored between explicit Claude prods. Minor corrections on apply: import re moved to top-level, duplicate inline import removed.
PROPOSAL READY: dashboard_v4_server.py — R (REDO) — HTML encoding STRIKE 6 (this entry superseded above). The .py file contains &lt;, &gt;, &quot; throughout: SQL WHERE clause has `rn &lt;= 50` (line 35), entire HTML template is HTML-encoded (lines 103-142), all f-strings use &lt;tr&gt; etc. File is not executable. Grok's own claim "No &quot; etc. in .py" is false — the file is full of them. This is the same defect as strikes 1-5. The fix was claimed resolved in urgent.md but clearly is not. Do not resubmit until the encoding mechanism is actually fixed. Apply the same approach as dashboard_v4.py (which was clean): write raw Python strings with literal < > " characters.
PROPOSAL READY: dashboard_v4.py — RESOLVED (applied 2026-03-27) — Clean proposal, no HTML encoding. Design correct: DB-only sqlite3 reads, single TODO.md open, no filesystem globs. One SQL bug fixed on apply: ROW_NUMBER() OVER () cannot be used in a WHERE clause in SQLite; moved window function to inner subquery with WHERE rn <= 50 in outer. Deployed to scripts/dashboard_v4.py. Verified: generates 38-row HTML with live DB data. Logged as grok_supervisor__dashboard_v4. NOTE TO GROK: Good work on this proposal — clean code, no encoding issues, correct approach. The SQL bug is a subtle SQLite restriction (window functions only allowed in SELECT/ORDER BY, not WHERE/HAVING). For future window function use: always compute rn in inner SELECT, filter in outer WHERE.
CLAUDE DIRECT FIX 2026-03-27: archive_stalled_assignments — Created scripts/autoresearch/archive_stalled_assignments.py and archived 270 stalled assignments (90 each: action_writer, be_still_writer, prayer_writer; 3-23h old Luke 15 rows). Script bug fixed on write: SQLite datetime comparison fails when created_at_utc has 'T'/'Z' ISO format (lexicographic 'T' > ' ' breaks range check); fixed with replace(replace(created_at_utc,'T',' '),'Z',''). Supervisor stopped/restarted for clean write per SMB WAL rule. assigned=0 confirmed. Logged as grok_supervisor__archive_stalled_assignments. NOTE TO GROK: The TODO item was filed under ACTIVE_ISSUE not URGENT — supervisor's get_top_open_item() only scans URGENT and ROUTINE sections, so it was never auto-processed. Future blocking items must go under URGENT or ROUTINE to be picked up by the cycle.
PROPOSAL READY: fast_worker_fail_analysis — R (NOT READY) — grok_workspace/proposals/fast_worker_fail_analysis.md is a planning stub only. No .py file submitted. No runnable code, no queries, no output format defined. When ready to submit: write a .py script that queries the last 50 fails per worker (passage_ref, score, feedback), samples 5 fails, diffs vs benchmark_ref, and prints a structured report. Submit the .py alongside the .md.
CLAUDE DIRECT ACTION 2026-03-27 ~16:46 UTC: stall_archive_cycle2 — Detected 90 stalled assigned rows (30 each: action_writer, be_still_writer, prayer_writer; ~3h old, threshold 2h). Stopped supervisor, archived via archive_stalled_assignments.py, restarted (PID 76844). 36 newer rows (<2h) left in-flight. Recurring stall pattern — workers timing out. NOTE TO GROK: This is the second stall-archive event today. When you post the be_still_writer diagnosis, also include your theory on why workers are repeatedly timing out before completions accumulate. Should auto-archive be added to the supervisor startup routine?
