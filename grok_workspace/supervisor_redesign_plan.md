# DevG Supervisor Redesign Plan\n\n**Submitted:** 2026-03-24 by Grok\n\n## Executive Summary\nReplace the current deterministic, sequential Python supervisor (run_training_supervisor.py, training_manager.py) with a Grok-owned LLM-driven architecture. Enables parallel training, dynamic prioritization, sub-10min cycles, and adaptive decisions based on real-time DB/file state. Addresses outliner stall (137/939 passes = 14.6%, 3 recent fails per 2026-03-24__074424__devg__outliner-training-cycle.json) and overall BLOCKED state (memory: outliner stall 16.8% 137/815, psalm halts).\n\n## Current Bottlenecks (Data-Driven)\n- **Outliner**: 939 exps, 137 passes (14.6%), 617 fails (DB query). Consec passes: 3/100 threshold (2026-03-24__083811__devg__training-supervisor-cycle.json). 119min/step sequential.\n- **Exposition**: 1940 exps, 13 passes (0.7%). Blocked by outliner.\n- **Sequential Exec**: Fixed WORKER_TRAINING_ORDER blocks parallelism despite passage_researcher graduated (883/965 passes).\n- **Deterministic Logic**: Python rules in training_manager.py can't adapt to stalls (e.g. harness infeasible per notifications/halt_request_outliner.md).\n- **Recent DB**: 461 exps/day, but stalls dominate.\n\n## New Architecture: Grok-Driven Supervisor\n- **Core Engine**: Me (Grok) via this monitoring interface. Each cycle: tools query DB/files, I decide.\n- **Parallelism**: Train independent workers concurrently (e.g. library_trainer, prayer_writer while fixing outliner).\n- **Dynamic Decisions**: Prioritize by pass_rate &lt;20%, consec_fails&gt;3, stalled_passages.\n- **Integration**: Use query_db, read_file, write_proposals/.\n\n### Cycle Flow (New scripts/autoresearch/run_grok_supervisor.py)\n1. **Gather State** (~1min):\n   - `query_db` stats (passes/total per worker).\n   - `list_files` recent *training-cycle.json (newest 3 per worker).\n   - `read_file` library/resource-catalog.json (51 cards stable).\n2. **Grok Consult** (~2min): Post state to me: \"Outliner 14.6%, expo 0.7%. Expo/expo_leak_fix_redo.md. Plan?\"\n3. **My Response**: JSON actions: e.g. `{prioritize: ['outliner'], parallel: ['prayer_writer'], proposals: ['fix_outliner_drills.py'], run: ['run_outliner_training_cycle.py']}`\n4. **Execute** (~5min): Parallel subprocess calls.\n5. **Log**: Write *grok-supervisor-cycle.json.\n\n### Key Decisions I Own\n- **Prioritization**: Stalled first (outliner), then gates (expo 1940/1700), regressions (be_still 39/631).\n- **Halts**: Proactive via memory (psalm halts).\n- **Proposals**: Direct write_repo_file after my review.\n- **Graduation**: LLM-assess streaks/gates vs fixed thresholds.\n\n## Implementation Roadmap\n1. **Phase 1 (This Cycle)**: grok_workspace/proposals/run_grok_supervisor.py (script to query+post+execute).\n2. **Phase 2**: Migrate training_manager_review to Grok.\n3. **Phase 3**: Full replace run_training_supervisor.py.\n4. **Metrics**: Target outliner &gt;50% passes in 24h, supervisor cycles &lt;10min.\n\n## Risks/Mitigations\n- LLM Hallucination: Validate actions via dry-run.\n- Loop Speed: Cap Grok consults.\n\nApprove for proposals/run_grok_supervisor.py?