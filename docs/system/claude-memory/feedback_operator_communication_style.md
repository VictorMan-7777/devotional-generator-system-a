---
name: Operator communication style — system-directed
description: Operator is moving toward system-directed language (state, blockers, actions) over human-oriented framing
type: feedback
---

Operator has noted they will work on making their communication more system-directed rather than human-oriented.

**Why:** System-directed language ("training is blocked pending three proposals") maps directly to state and actions. Human-oriented framing ("what is he doing?", "good work") requires Claude to translate intent before routing work.

**How to apply:**
- When operator uses system language, act on it directly without asking for clarification
- When operator uses human-oriented framing, translate it to system state internally — but ask a clarifying question if the intent is genuinely ambiguous rather than guessing
- Match the operator's register: respond with system state and actions, not social acknowledgment
- The exchange: operator gives clearer inputs, Claude asks questions when unclear rather than inferring
